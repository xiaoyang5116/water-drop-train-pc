import { Button } from 'antd';

const Home = () => (
  <div className="container">
    <Button onClick={() => {}}>
      个人中心
      {/* {store.tel} */}
    </Button>
  </div>
);

export default Home;
